<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectlogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HelloNext"></activity>
        <activity android:name=".ProjectOption" />
        <activity android:name=".View" />
        <activity android:name=".ProjectView" />
        <activity android:name=".ProjectUpload" />
        <activity android:name=".ProjectHome" />
        <activity android:name=".ProjectRegister">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Hello" />
    </application>

</manifest>